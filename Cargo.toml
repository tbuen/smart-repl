[package]
name = "smart-repl"
version = "0.1.0"
edition = "2024"

[dependencies]
colored = "3"
log = "0.4"
rustyline = { version = "18.0", default-features = false, features = ["derive"] }

[lints.clippy]
pedantic = "warn"
